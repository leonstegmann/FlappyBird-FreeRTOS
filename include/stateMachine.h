/* Project includes  */
#include "states.h"

/**
 * @brief This function has to be modifierd if new stages are added.
 * It uses: 
 *  states_add();
 *  states_init();
 *  states_set_state();
 * from the states.h library to Setup the StateMachine. 
 * @return 0 on Succes, 1 on Error.
 */
int initStateMachine();